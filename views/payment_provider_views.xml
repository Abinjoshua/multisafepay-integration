<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form_multisafepay" model="ir.ui.view">
        <field name="name">payment.provider.form.multisafepay</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id"
               ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position="inside">
                <!-- Add API key / secret fields here for a real gateway -->
                <field name="multisafepay_api_key" string="API"/>
            </group>
        </field>
    </record>
</odoo>
